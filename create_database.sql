-- Create database if it doesn't exist
CREATE DATABASE IF NOT EXISTS healthcard_db;

-- Use the database
USE healthcard_db;

-- Show confirmation
SELECT 'Database healthcard_db created successfully!' AS Status;

-- Show all databases
SHOW DATABASES;
